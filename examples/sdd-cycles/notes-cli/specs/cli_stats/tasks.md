# Tasks — cli_stats

Lista de tareas necesarias para la implementación del comando `stats`.

- [x] T1 — Implementar función `cmdStats` en `src/cli.js`. Cubre: R1, R2, R3, R4, R5.
- [x] T2 — Registrar subcomando `stats` en `COMMANDS` dentro de `src/cli.js`. Cubre: R1, R2, R3, R5.
- [x] T3 — Añadir test de base de datos vacía para `stats` en `tests/test_cli.js`. Cubre: R3.
- [x] T4 — Añadir test de base de datos con una nota para `stats` en `tests/test_cli.js`. Cubre: R1, R2, R4.
- [x] T5 — Añadir test de base de datos con múltiples notas para `stats` en `tests/test_cli.js`. Cubre: R1, R2, R4.
- [x] T6 — Añadir test de argumentos adicionales inválidos para `stats` en `tests/test_cli.js`. Cubre: R5.
